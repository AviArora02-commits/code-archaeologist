"""Math utilities for testing."""

def add(a: int, b: int) -> int:
    """Add two numbers — introduced for rate-limit testing."""
    return a + b


class Calculator:
    def multiply(self, x: int, y: int) -> int:
        return x * y
